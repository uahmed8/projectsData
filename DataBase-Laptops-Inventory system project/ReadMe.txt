-SQL File will have to be run first so the tables that are being used in the project will get access to data.
After execution there will be some inserted into the login Table. records for users: Admin,Employee,Customer.

Usernames and Password:
		Admin
			User Name = Admin
			Password = Admin
		Employee
			User Name = MMalik
			Password =  Pass 
		Customer
			User Name = SL221
			Password  = Pass


-Admin will have access to all files, Shops can be added by Admin and there owners will also be added by Admin.
-Owners can add employees and items into their shop, and Employee won't be able to get access to further ordering detail part.
-Customers will have access to items and will be able to place orders into the shoppingcart.
 

 